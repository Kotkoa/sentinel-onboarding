import type { ClientRecord, ClassificationResult, Finding } from '../model/types'

export function detectFindings(
  _record: ClientRecord,
  _classification: ClassificationResult,
): Finding[] {
  return []
}
